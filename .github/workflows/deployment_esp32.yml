name: Deploy LS Sensor ESP32 on Environment

on:
  workflow_dispatch:
    inputs:
      serial_port:
        required: false
        type: string
      environment:
        required: true
        type: choice
        options:
          - Home

permissions:
  contents: read
  deployments: write

jobs:
  deployment:
    uses: Zefek/GithubActions/.github/workflows/arduino-build-deploy.yml@d0f9e15e0cee0d6a5ab842556b55ac419cc4d3ac
    with:
      environment: ${{ inputs.environment }}
      serial_port: ${{ inputs.serial_port }}
      device_name: LSSensorESP32
      source_dir:  "./src/ESP32"
      ota:         true
